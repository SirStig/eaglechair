# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
# Actual environment files (contain secrets or runtime config)
.env
.env.local
.env.*.local

# Keep these committed (safe defaults):
# .env.example (template)
# .env.development (dev defaults)
# .env.production (prod defaults)

# Legacy env files (deprecated but keep gitignored)
demo.env
